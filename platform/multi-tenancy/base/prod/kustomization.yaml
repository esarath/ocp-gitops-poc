apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - networkpolicy.yaml
  - rolebinding.yaml
# resourcequota.yaml and limitrange.yaml are NOT here — see
# platform/multi-tenancy/manual/README.md for why.
